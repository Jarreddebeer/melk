# A notification subsystem. Same pattern as payments.melk — a small
# internal pipeline addressable via qualified refs from the parent.

layout: lr

receive { size: 7x5 }
dispatch { size: 9x5 }
deliver { size: 7x5 }
pipeline route: receive -> dispatch -> deliver
